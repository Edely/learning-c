#include <stdio.h>
#include <stdlib.h>

int main(){
    //Inicializando um array com zeros
    int vet[5], i;
    for(i = 0; i <5; i++){
        vet[i] = 0;
    }
    
    system("pause");
    return 0;
}