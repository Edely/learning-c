#include <stdio.h>
#include <stdlib.h>

int main(){
    /* Podemos definir uma lista de valores para colocar no array */
    //Forma geral:
    tipo nome_arra[tam1]...[tamN] - {valores};

    //Exemplo:
    int vet[5] = {15, 12, 9, 1, 35};

    system("pause");
    return 0;
}