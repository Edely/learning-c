#include <stdio.h>
#include <stdlib.h>

int main(){
    /* String: um arrray de caracteres permite armazenar uma sequência de caracteres adjancentes na memória (palavras ou frases) */

    //Forma geral:
    char some_array(tamanho);

    //Exemplo:
    char palavra[20];

    system("pause");
    return 0;
}