#include <stdio.h>
#include <stdlib.h>
int main(){
    Variáveis:
    tipo_variavel nome_variavel;
    int x;

    Ponteiros:
    tipo_ponteiro *nome_ponteiro
    int *y;

    int x, y;

    /* Ponteiros n inicializados
    apontam para um lugar indefinido */
    int *p;

    /* Um ponteiro pode ter um valor 
    especial NULL, que é o endereço de 
    nenhum lugar. */
    int *p = NULL;
    
    system("pause");
    return 0;
}