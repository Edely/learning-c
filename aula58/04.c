#include <stdio.h>
#include <stdlib.h>
int main(){
    /* Também podemos declarar um 
    array de ponteiros*/
    tipo_dado *nome_array[tamanho];
    
    int *vet[5]; //array de 5 ponteiros
        
    system("pause");
    return 0;
}