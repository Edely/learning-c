#include <stdio.h>
#include <stdlib.h>
int main(){
    /* função malloc():
    serve para alocar memória durante a
    execução do programa.
    Ela faz o pedido de memória ao computador
    e retorna o ponteiro com o endereço 
    do início do espaço de memória alocado. */
    void* malloc(unsigmed int num);
        
    system("pause");
    return 0;
}