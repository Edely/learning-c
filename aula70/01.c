#include <stdio.h>
#include <stdlib.h>
int main(){
    /* Para ler um caractere de um
    arquivos usamos a função fgetc() */
    // Forma Geral 
    int fgetc(FILE *fp);

    /* retorna 
    - Em caso de erro: a constante EOF
    - Em caso de sucesso: o caractere lido 
    do arquivo */
    
    system("pause");
    return 0;
}