#include <stdio.h>
#include <stdlib.h>
int main(){
    /* Porém, quando manipulando dados
    binários, um valor inteiro igual 
    ao valor da constante EOF pode ser lido
    
    Para evitar este tipo de situação,
    a lgg C inclui a função feof()

    int feof(FILE *fp);

    Essa função retorna um valor inteiro 
    igual a ZERO se ainda não tiver
    atingido o final do arquivo
    */

    system("pause");
    return 0;
}