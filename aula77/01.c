#include <stdio.h>
#include <stdlib.h>

int main(){
    Nas últimas aulas vimos como ler e
    escrever em arquivos caracteres, 
    strings e até mesmo blocos de bytes.

    A lgg C tb permite escrever
    uma lista formatada de variáveis em
    arquivo do mesmo modo como fazemos na tela
    do computador com o função printf()

    Para isso usamos a função fprintf()

    system("pause");
    return 0;
}