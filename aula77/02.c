#include <stdio.h>
#include <stdlib.h>

int main(){
    //Forma geral: printf()
    printf("tipos de saída", variáveis);

    //Forma geral: fprintf()
    fprintf(FILE *f, "tipos de saída", variaveis);
    
    system("pause");
    return 0;
}