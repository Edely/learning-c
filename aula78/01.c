#include <stdio.h>
#include <stdlib.h>

int main(){
    Nas últimas aulas vimos como ler e 
    escrever em arquivos caracteres,
    strings e até mesmo blocos de bytes.

    A lgg C tb permite ler uma 
    lista formatada de variáveis do arquivo
    do mesmo modo como fazemos do teclado 
    do computador com a função scanf()

    Para isso usamos a função fscanf()
    
    system("pause");
    return 0;
}