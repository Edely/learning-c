#include <stdio.h>
#include <stdlib.h>

int main(){
    //Forma geral: scanf()
    scanf("tipos de entrada", variáveis);

    //Forma geral: fscanf()
    fscanf(FILE *f, "tipos de entrada", variaveis);
    
    system("pause");
    return 0;
}