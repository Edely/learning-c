#include <stdio.h>
#include <stdlib.h>

int main(){
    
    /* Outra opção de movimentação dentro 
    arquivo é simplesmente retornar para 
    o seu início usando a função rewind() */

    //Forma geral
    void rewind(FILE *fp);
    
    system("pause");
    return 0;
}