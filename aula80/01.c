#include <stdio.h>
#include <stdlib.h>

int main(){

    Ao longo das aulas, vimos que o main() indicava a função principal
    do programa. Ela é responsável pelo início da execução do programa.

    A função main() também pode receber uma lista de parâmetros no 
    início da execução do programa.

    Esses parâmetros virão do prompt da linha de comando do sistema
    operacional.

    system("pause");
    return 0;
}